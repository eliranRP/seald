# Sealed — Dashboard UI Kit

Document list with filters, stat row, and table. Reuses `TopNav`, `LeftRail` from the signing app kit. The table is the workhorse — status column is always a `Badge`, doc IDs are always mono.
