# Sealed — Mobile App UI Kit

Three iOS screens: Inbox → Document view → Sign. Uses the iOS 26 device frame and the same `SignatureMark` / `Badge` / `DocThumb` as the desktop kit. Bottom-fixed primary action on each step.
